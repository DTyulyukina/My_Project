import { EventEmitter } from '@angular/core';
import { BusyService } from './busy.service';
export declare class BusyButtonComponent {
    busy: BusyService;
    cssClass: string;
    disabled: boolean;
    icon: string;
    onClick: EventEmitter<boolean>;
    constructor(busy: BusyService);
    send(): void;
}
